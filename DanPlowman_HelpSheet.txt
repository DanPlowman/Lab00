C++ code can be commented two ways, either as a block starting with /* then ending with */, or as a single line with //
Python code is commented in lines with a #
.md files are called "Mark Down" files
To create a pointer, you literally create an arrow in ascii characters "->" this points to the value.
to create an object of type T you would Type "T * 'objectname';
